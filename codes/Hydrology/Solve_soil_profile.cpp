#include "Basin.h"

int Basin::Solve_soil_profile(Control &ctrl, Param &par){

    for (unsigned int j = 0; j < _sortedGrid.row.size(); j++) {

        //infiltration_1(Control &ctrl, Param &par);

    }

    return EXIT_SUCCESS;
}
